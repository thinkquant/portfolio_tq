# Local web development
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:8080

# Optional override for direct API calls when not using the local proxy.
# Keep relative /api paths by default so the Vite dev server can proxy locally.
VITE_API_BASE_PATH=/api
